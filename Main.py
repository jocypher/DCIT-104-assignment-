# A python program to find the average of the sum the number of prime numbers from 1 to 10
prime_numbers = [2,3,5,7]
sum = prime_numbers[0] + prime_numbers[1] + prime_numbers[2]
prime = sum + prime_numbers[3] 
print(prime)
  
  # finding the average 
  
primes = len(prime_numbers )
aver = prime/ primes
print(aver)
